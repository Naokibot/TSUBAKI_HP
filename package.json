{
  "name": "hotaru-ascend-portfolio",
  "version": "4.1.0",
  "private": true,
  "type": "module",
  "description": "A multilingual Hotaru Ascend portfolio for GitHub Pages.",
  "scripts": {
    "validate": "npm run blog:sync && node scripts/validate-release.mjs",
    "check": "npm run validate && node --check src/app.js && node --check scripts/build.mjs && node --check scripts/blog.mjs && node --check scripts/validate-release.mjs && node --check scripts/serve.mjs",
    "build": "npm run validate && node scripts/build.mjs",
    "build:pages": "SITE_BASE_URL=https://naokibot.github.io/TSUBAKI_HP/ SITE_BASE_PATH=/TSUBAKI_HP npm run build",
    "serve": "node scripts/serve.mjs",
    "dev": "npm run build && npm run serve",
    "preview": "npm run dev",
    "blog:new": "node scripts/blog.mjs new",
    "blog:sync": "node scripts/blog.mjs sync"
  },
  "engines": {
    "node": ">=20"
  }
}
